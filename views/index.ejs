<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head') %>
    <body data-spy="scroll" data-target=".fixed-top">
        <!-- Navigation -->
        <%- include('./partials/navbar', {currentPage: 'home'}) %>
        <!-- end of navigation -->
        
        <div class="container-fluid d-flex flex-column justify-content-center align-items-center vh-100">
            <h1 class="text-center">Welcome to Rishon Bootcamp!</h1>
            <h5 class="mt-3 mb-auto">Use <span class="text-primary">navbar</span> to navigate between pages.</h5>

            <!-- Buttons for Sign Up, Log In, and Enter as Guest -->
            <div class="mt-4">
                <a href="/register" class="btn btn-primary btn-lg mx-2">Sign Up</a>
                <a href="/login" class="btn btn-secondary btn-lg mx-2">Log In</a>
                <a href="/map" class="btn btn-outline-info btn-lg mx-2">Start Planning as One-time User</a>
            </div>
        </div>
    </body>
</html>
